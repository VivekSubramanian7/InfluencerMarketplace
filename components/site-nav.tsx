/**
 * @deprecated Use `AuthenticatedShell` from `@/components/authenticated-shell` instead.
 * SiteNav was the pre-workspace sticky header; authenticated pages now use AppShell.
 */
export { AuthenticatedShell as SiteNav } from "@/components/authenticated-shell";
